{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // This connection string is for a local PostgreSQL database.
    //"PostgresConnection": "Server=localhost; Database=eCommerceUsers; Username=postgres; Password=admin",
    // This connection string is for a docker PostgreSQL database when connecting locally.
    //"PostgresConnection": "Host=localhost;Port=5433;Username=postgres;Password=mysecretpassword;Database=eCommerceUsers"
    // This connection string is for a docker PostgreSQL database when doing image build for docker.
    "PostgresConnection": "Host=postgres-user-db;Port=5432;Username=postgres;Password=mysecretpassword;Database=eCommerceUsers"
  }
}
